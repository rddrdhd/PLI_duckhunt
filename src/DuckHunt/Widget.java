package DuckHunt;

import javafx.scene.paint.Color;

public class Widget extends Sprite {
    private static Sprite bar;
    Widget(int x, int y, int w, int h, String type, Color color) {
        super(x, y, w, h, type, color);
    }
}
